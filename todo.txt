Todo list.  Open items only - finished work is in CHANGELOG.md and in the commit
messages that made the changes, not kept here.  Checks that need a person at a
keyboard are in MANUAL_CHECKS.md.  Questions that need the owner's ruling - not
a check to run but a call to make - are in DECISIONS.md, because a [DECISION]
tag was never a machine capability and so broke the promise the next paragraph
makes about every tag in this file.

Every item carries a tag saying what a machine has to be able to do to take it,
so a session on another OS can see at a glance what it can pick up.

EVERY ITEM IS WORK IN THIS REPOSITORY.  An item whose work would be done in
romwbw_disks, z80cpmw, ioscpm or cpmdroid belongs in THAT repository's
todo.txt, where the session that can close it will read it - filing it here
makes a list nobody can act on and that nobody closes, because the person
finishing the work is not looking at this file.  That is what 342ac22
("refile two items into the repos that would close them") established, and
two entries were filed here against it on 2026-09-17 and removed the same
day: one about the three GUI clients dropping the release gate, and one about
romwbw_disks' documentation of it.  Both were real work; neither was this
repository's.

A cross-repository CONSEQUENCE is different from a cross-repository TASK, and
belongs in DOWNSTREAM.md - which is exactly where "what a port must change"
already lives.

WHAT THIS MACHINE ACTUALLY HAS - and WHICH machine, because that was the trap.

This block is a measurement of one computer, and it used to say only "THIS
MACHINE" while describing a Mac.  A session reading it on anything else got
confident wrong answers about brew paths, ~/Library and /usr/bin/python3, and
the tags in front of every item below - [EMSCRIPTEN], [BROWSER], [WINDOWS] -
are read against it.  So it now names the machine it measured.

  measured on   2026-09-19, on Windows 11 (26200) under git-bash, from
                C:/temp/src/romwbw_emu.  The block before this one measured a
                macOS machine on 2026-09-18; nothing here contradicts it,
                they are two different computers.  If you are on neither,
                re-measure before believing a row.

  python3       YES, 3.13.14 - the Microsoft Store build, reached through the
                WindowsApps shim.
  um80/ul80     YES, 0.3.40, at
                ~/AppData/Local/Packages/PythonSoftwareFoundation.Python.3.13_
                qbz5n2kfra8p0/LocalCache/local-packages/Python313/Scripts.
                NOT on PATH; put that directory on it.

                AND IT WAS BROKEN IN A WAY `pip install um80` DOES NOT FIX.
                An editable install pointed at C:/temp/src/um80_and_friends,
                a checkout that is gone, so only the .exe shims and a
                .dist-info were present, `import um80` failed, and pip
                reported the requirement already satisfied and did nothing.
                `pip install --force-reinstall um80` is what repaired it on
                2026-09-19.  If um80 exits with ModuleNotFoundError, that is
                this, and re-running the install from CLAUDE.md will not say
                so.
  C++           g++ 16.1.0 (MinGW-W64 UCRT), with nm, cmake 4.4.1, ninja and
                mingw32-make.  There is NO `make`: the target is
                `mingw32-make`, and `make -C src test` does not exist here.
  the CLI       DOES NOT BUILD on this machine.  romwbw_emu.cc includes
                <termios.h> and MinGW has none, so `mingw32-make -C src` dies
                on the first object.  It is the CLI FRONT END that is POSIX,
                not the core, and not the tests.
  the C++ tests SEVEN OF NINE RUN HERE, and do not need the CLI.  The test
                programs link DISPATCH_OBJS - emu_io_common, hbios_dispatch,
                hbios_cpu, emu_init - and nothing else; only the `test:`
                target's dependency on the romwbw_emu binary made it look
                otherwise, and this block said "the C++ tests cannot run here"
                on the strength of that until 2026-09-19.  Build them one at a
                time and skip the binary:

                    cd ../cpmemu/src && mingw32-make libqkz80.a
                    cd ../../romwbw_emu/src
                    for t in vda_keyboard hbios_hostname snd_channels \
                             sys_bnkcpy rtc_settim sys_switch dio_units; do
                      mingw32-make test_$t && ./test_$t.exe
                    done

                All seven pass.  cli_console and cli_hostfile are the two that
                genuinely cannot run: a pty and a POSIX filesystem.  Running
                them here found the 32-bit `long` RTC overflow that z80cpmw
                and cpmdroid had shipped - see CHANGELOG.md.  cl builds and
                runs the same seven; the .bat that does it is in the msvc job
                of .github/workflows/test.yml.
  WSL           Ubuntu is INSTALLED and starts, at /mnt/c/temp/src, but has
                python3 and NOTHING ELSE - no g++, no make - and sudo wants a
                password, so this session could not add them.  That is the one
                route to the POSIX front end and the last two tests on this
                machine:  wsl -d Ubuntu -- sudo apt install build-essential
  cl            YES, 14.51.36231, under Visual Studio 18 Community.  NOT on
                PATH - it needs a developer prompt, or the vcvars script.
                MSBuild IS on PATH.  [WINDOWS] is measurable here, and this
                is the machine z80cpmw's vcxproj is for.
  node / npm    NO.  The web JS tests cannot run here.
  emcc          NO, and no emsdk checkout.  [EMSCRIPTEN] items cannot be
                taken on this machine.
  Chrome        YES, at "C:/Program Files/Google/Chrome/Application".
  gh            YES, 2.83.2, and authenticated: `gh release list` answers.
  sibling repos ALL PRESENT beside this one: cpmemu, romwbw_disks, ioscpm,
                z80cpmw, cpmdroid, z80fpga.  So a claim about what a downstream
                port does can be checked here rather than assumed - which is
                how four stale facts in DOWNSTREAM.md were settled on
                2026-09-19.
  newest tag    v1.47, and it IS released - `gh release list` shows v1.47 as
                Latest, cut 2026-09-18.  This said "v1.42, and there is NO
                v1.43 tag: WIP.md records that v1.43 was committed and the
                GitHub release never cut, blocked on a dead PAT."  Five tags
                have been cut since, the PAT problem is over, and WIP.md
                itself is gone (4086092, "the one thing it was waiting on is
                released").

NO OPEN ITEMS.  That is the state, not a file someone forgot to write in:
the last one - hide prerelease:true entries behind an opt-in - closed on
2026-09-19.  An empty list here does not mean there is no work; it means
nothing is written down that a machine could pick up unprompted.  Look in
MANUAL_CHECKS.md for what needs a person, DECISIONS.md for what needs the
owner's ruling, and the sibling repositories' own todo.txt for work this
one cannot close.
