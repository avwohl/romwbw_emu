Todo list.  Open items only - finished work is in CHANGELOG.md and in the commit
messages that made the changes, not kept here.  Checks that need a person at a
keyboard are in MANUAL_CHECKS.md.  Questions that need the owner's ruling - not
a check to run but a call to make - are in DECISIONS.md, because a [DECISION]
tag was never a machine capability and so broke the promise the next paragraph
makes about every tag in this file.

Every item carries a tag saying what a machine has to be able to do to take it,
so a session on another OS can see at a glance what it can pick up.

- [ANY] src/emu_hbios.asm is bank 0 of every published ROM, and romwbw_disks
carries a copy that tools/check_source_drift.sh requires to assemble to the same
512 KB ROM.  so ANY edit here moves every ROM's sha256, which bumps each
romwbw_disks version's generation, and a generation bump is what makes a client
invalidate its cached images - every user re-downloads their disks.  that is not
theoretical: HB_BNKCALL (e47c948, d4f4a2a, 2026-09-06) cost exactly that, a
generation-2 republish of both 3.5.1 and 3.6.0.  budget it before the next
emu_hbios.asm change.  the finished HB_BNKCALL work is CHANGELOG.md's `### HB_BNKCALL performs a real
bank call` section (grep it; CHANGELOG grows at the top, so a line number will
not stay right).
  UPDATE 2026-09-15: the comparison runs again, and is stronger than the one it
replaced.  romwbw_disks fe08145 (2026-09-07) dropped the read of
romwbw_emu/roms/emu_avw.rom - which v1.40 deleted - and now assembles BOTH
trees' emu_hbios.asm and compares the 32 KB bank 0, plus builds this tree's
src/{r8,w8}.asm and compares against build/utils/.  No ROM, no image, no
network.  Its comparison of the four .asm sources is why those four stay here.
what replaces the ROM half here is roms/build_emu_rom.sh, which assembles bank 0
and compares the result against the sha256 the catalog publishes - a stronger
question, because it is asked against what users download rather than against a
second copy in a sibling checkout.  the generation cost above is unchanged: an
edit here still moves every published ROM and still makes every user re-download.

- [BROWSER] no browser has drawn the page since xterm was vendored, since a
zero-byte export started downloading, and since the download name became the
lowercased last component of the typed path.  the checklist is in
MANUAL_CHECKS.md.  the wasm is no longer the blocker, and it no longer needs a
CI artifact: the v1.38 release ships romwbw-emu_1.38_amd64.deb, which carries
web/romwbw.html, romwbw.js, romwbw.wasm, vendor/ and emu_avw.rom as a servable
layout - `gh release download v1.38 -R avwohl/romwbw_emu -p
'romwbw-emu_1.38_amd64.deb'` then `dpkg-deb -x`.  the packaged romwbw.html is
HEAD's template rendered apart from the three @VERSION@ substitutions, which say
1.38 where VERSION now says 1.39.  its emu_avw.rom is c7abc580, the ROM from
before e47c948, so anything that exercises the bank call answers for the old
one.  serve the UNPACKED DEB, not web/ out of
this tree: web/ held gitignored March leftovers of romwbw.js and romwbw.wasm
whose md5s differed from the CI build's, and serving them would have answered
all three checks against a five-month-old wasm.  they are deleted (make -C web
clean), as are the December 2025 romwbw_node.* and romwbw_test.* pairs that
`clean` does not name - there is no .wasm left in web/ at all, so `make serve`
and both deploy targets now fail on absent emcc instead of shipping a stale one.
what is left is a person at a browser: emcc, node and every browser are absent
from this machine.
  UPDATE 2026-09-15: the packaged page answers for HEAD again.  v1.41
(2026-09-10) and v1.42 (2026-09-13) both ran release.yml's emcc build, and the
v1.42 amd64 deb's usr/share/romwbw_emu/web/romwbw.html is byte-identical to
`sed s/@VERSION@/1.42/ web/romwbw.html-template`, with a freshly built
romwbw.wasm and romwbw.js beside it and vendor/ staged.  Nothing under src/ or
web/ has changed since the v1.42 tag, so that wasm is HEAD's core.  It carries
no ROM, as designed, so `romwbw-get mirror` has to be run beside the page
before it will boot anything.  Download the v1.42 deb and serve that, rather
than the stale v1.38 one this entry used to name.

- [BROWSER] the wasm's video, DSKY, sound and printer callbacks are wired to
names nothing implements.  emu_io_wasm.cc emits Module.onVideo*, Module.onDsky*
and Module.onPrinterOutput; romwbw.html-template implements Module.onVda* and
Module.onSnd*.  renaming one side does not switch it on: there is no sound
emitter in the core at all, five of the eight onVda* handlers have no
counterpart, and onVdaWriteChar takes an attribute byte the emitter never
sends.  about 200 lines on each side that have never executed - wire it
properly behind a browser, or delete one side.

- [BROWSER] nobody has opened the page in a browser, and four things in it
are new.  the ROM, disk and RomWBW-release selects are built from
catalog/manifest.json at load time rather than from hardcoded <option> lists;
each download is checked against the manifest's size and sha256 through
crypto.subtle, which needs a secure context and says so rather than claiming a
pass it did not perform; a directory with no mirror in it gets an explanatory
status line and working file pickers instead of a 404; and the RomWBW select
disables a release this build cannot boot.  all four are covered by
tests/web_manifest.js under node - though `make -C src test` SKIPs it on this
machine, because node is not on PATH - and none has been DRAWN.  Chrome and
Safari are both installed here; what is missing is emcc, so the page cannot be
built locally.  The wasm HAS been rebuilt since the rewrite: release.yml builds
it with emcc at every release, and v1.41 and v1.42 both shipped one.  what this needs is one
person on a machine with emcc and the checklist in MANUAL_CHECKS.md.  `make -C
web serve` mirrors emu_avw and hd1k_combo into web/ before starting the server,
so a local serve has a bootable ROM and disk for the first time - that half is
worth checking too, because nothing has ever exercised it.

- [EMSCRIPTEN] the web Boot field never reads NVRAM back.  the page pushes the
string with _romwbw_set_boot_string and clears it with _romwbw_clear_nvram, and
web/makefile's EXPORTED_FUNCTIONS holds no getter, so a boot target the guest
itself writes with SYSCONF is lost on reload - the text survives only in
localStorage.  the CLI has load_nvram_setting/save_nvram_setting under
$XDG_CONFIG_HOME/romwbw_emu/nvram and the web build has no equivalent read
path.  needs emcc to build and a browser to check.

- [EMSCRIPTEN] R8's "Reading:" line names the file that was really opened on
the CLI only.  emu_host_file_get_read_name() returns "" in emu_io_wasm.cc on
purpose - a browser read is a file PICKER, so the guest's string is a hint the
user is free to ignore - and the comment there carries the whole argument.
changing it needs a name parameter on emu_host_file_provide_data() and a change
to the page's picker callback.  until then the browser answers 0xFF and R8
prints what was typed, which is what it always did.

- [EMSCRIPTEN] the page decides which RomWBW releases it can boot from a value
recorded at mirror time, not by asking the core it is running.  `romwbw-get
mirror` runs the CLI binary's --version and writes the answer into
catalog/manifest.json as emu_supported; the page disables anything outside that
list.  so the two can disagree - a mirror written by one build, served next to a
wasm compiled from another - and the page would then offer a release the wasm
refuses, or grey out one it would run.  the honest source is
emu_romwbw_supported_list(), which already exists in src/emu_init.h and
src/emu_init.cc, and emu_init.cc is already in web/makefile's ROMWBW_SRCS, so
this is one name added to EXPORTED_FUNCTIONS plus a few lines in the page.  it
was not done because it cannot be built or checked without emcc, which is absent
here, and shipping an untested export is worse than the mismatch it fixes.
tagged [EMSCRIPTEN] rather than [ANY] for that reason.
what makes it worth doing rather than merely correct: the whole point of the
mirror is that re-running it publishes a newly released disk with no wasm
rebuild.  the moment somebody re-mirrors without rebuilding - which is the
intended workflow, not an accident - emu_supported is a fact about a binary that
is no longer the one in the page.  do it the next time emcc is on the machine.

- [RELEASE] a new RomWBW release needs a tag here, not just a catalog entry:
the releases a client will offer are filtered by ROMWBW_SUPPORTED_RELEASES,
which is compiled into this core, so a release nobody has added here is one no
client can boot however the catalog is cut.

- [ANY] docs/ROM_ATTESTATION.md is stale in three places and none of them is
safe to fix unasked.  it lives here rather than in DECISIONS.md because the work
is a document edit any machine can make; what is missing is agreement on the
wording, not a capability.  it is a signed filing to Apple - "I hereby
affirm", "I hereby grant Apple Inc. permission", "Digital Signature" - so the
signed text is not something to reword quietly, and the v1.40 migration
deliberately touched none of it.  three things in it are now out of date.  its
Component 2 says "Version: 3.5.1", which was the whole truth when a single
pinned ROM shipped and is narrower than what the
binary supports since v1.39 (3.5.1 and 3.6.0, and the version a guest sees is
read from the ROM it loads).  its "Date: December 2024" is two years stale.  and
ioscpm carries a near-identical copy of the same document that differs from this
one only in that date, so there are two signed filings and no rule about which
is the original.  the ruling is whose document it is, whether it is re-signed
today naming both releases, and whether one of the two copies goes.  what did
not need a ruling has been done: a dated note below the signature records that
the ROM has left this repository and that roms/build_emu_rom.sh reproduces the
published bytes and says so, which is what makes the GPL and attestation claim
checkable rather than asserted.  that note is additive and stops at the
signature line; everything above it is untouched.

- [WINDOWS] nothing on Windows is RUN, only compiled.  the msvc job builds the
three core .cc files z80cpmw pulls straight out of src/ and stops there: there
is no MSVC build of the emulator to link and no test program that would run.
the only thing that executes Windows code is z80cpmw built and run by hand on a
Windows box (build.bat, run_test.bat, tests/): that port has no CI at all -
`gh api repos/avwohl/z80cpmw/actions/workflows` answers 0 and `gh run list` is
empty.  cpmemu is no longer in that position: 43bff79 added
.github/workflows/ci.yml on 2026-09-03, whose ubuntu and macos jobs run
tests/run_tests.sh --require and whose windows-latest job builds with MSVC and
then runs tests\win_console.bat under CPMEMU_REQUIRE_MSVC=1 - the same batch
file that suite's "windows console" section shells out to, so those cases are
finally executed by a machine, and the file's header says that job is the first
one ever to run them.  the other of that suite's two Windows sections, "windows
cross-compile", skips merely for want of x86_64-w64-mingw32-g++, which IS
installed here, so it runs and is another compile rather than a run.  what is
still only compiled and never run is this repository's own core.

- [SOUND] the SND handler stores four channels and plays one beep.  handleSND
in src/hbios_dispatch.cc keeps snd_period[4] and snd_volume[4], and
BF_SNDQUERY/SNDQ_CHCNT answers "4 tone channels, 0 noise" - but nothing reads
those arrays back out.  BF_SNDPLAY looks at channel 0 only and calls
emu_dsky_beep(duration), which is a fixed beep of that length: the period is
not a pitch and the other three channels are inaudible.  a guest playing a
four-voice tune hears four identical beeps in sequence.  SNDQ_DEV already
declines to name a chip for exactly this reason - "there is no sound chip here
- nothing reads snd_period or snd_volume back out".
  WHAT WOULD CLOSE IT is rendering the channels the handler already records:
period -> frequency, volume -> amplitude, mixed across the four.  that is one
AY-3-8910/YM2149 worth of square-wave generation and it belongs here, in the
shared core, not in any one front end - all four ports would get it from the
same change, and only the output side (AudioTrack, AVAudioEngine, WASAPI, Web
Audio) is per-client.
  IT NEEDS A NEW emu_io ENTRY POINT.  src/emu_io.h declares exactly one audio
hook, `void emu_dsky_beep(int duration_ms)`, and a duration is the whole of its
vocabulary - there is no way to hand a front end samples or a pitch through it.
so this is a DOWNSTREAM.md-sized change, not a local one: adding the hook is
the easy half and four ports implementing it is the rest.  a front end that
does not want it must keep working, so the new hook needs a default the way the
optional ones already here do.
  requested by a user on a physical tablet against cpmdroid, where it sat in
docs/bug1.txt until 2026-09-15.  cpmdroid cannot close it alone.
