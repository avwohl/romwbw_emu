Todo list.  Open items only - finished work is in CHANGELOG.md and in the commit
messages that made the changes, not kept here.  Checks that need a person at a
keyboard are in MANUAL_CHECKS.md.  Questions that need the owner's ruling - not
a check to run but a call to make - are in DECISIONS.md, because a [DECISION]
tag was never a machine capability and so broke the promise the next paragraph
makes about every tag in this file.

Every item carries a tag saying what a machine has to be able to do to take it,
so a session on another OS can see at a glance what it can pick up.

- [ANY] src/emu_hbios.asm is bank 0 of every published ROM, and romwbw_disks
carries a copy that tools/check_source_drift.sh requires to assemble to the same
512 KB ROM.  so ANY edit here moves every ROM's sha256, which bumps each
romwbw_disks version's generation, and a generation bump is what makes a client
invalidate its cached images - every user re-downloads their disks.  that is not
theoretical: HB_BNKCALL (e47c948, d4f4a2a, 2026-09-06) cost exactly that, a
generation-2 republish of both 3.5.1 and 3.6.0.  budget it before the next
emu_hbios.asm change.  the finished HB_BNKCALL work is CHANGELOG.md:586.
  UPDATE 2026-09-07: the ROM comparison that would have caught the two copies
diverging no longer runs.  check_source_drift.sh reads
romwbw_emu/roms/emu_avw.rom, which v1.40 deleted, so its artifact half prints an
info line and the script still exits 0; its comparison of the four .asm sources
is untouched and still works, and is why those four sources stay in this tree.
what replaces the ROM half here is roms/build_emu_rom.sh, which assembles bank 0
and compares the result against the sha256 the catalog publishes - a stronger
question, because it is asked against what users download rather than against a
second copy in a sibling checkout.  the generation cost above is unchanged: an
edit here still moves every published ROM and still makes every user re-download.

- [BROWSER] no browser has drawn the page since xterm was vendored, since a
zero-byte export started downloading, and since the download name became the
lowercased last component of the typed path.  the checklist is in
MANUAL_CHECKS.md.  the wasm is no longer the blocker, and it no longer needs a
CI artifact: the v1.38 release ships romwbw-emu_1.38_amd64.deb, which carries
web/romwbw.html, romwbw.js, romwbw.wasm, vendor/ and emu_avw.rom as a servable
layout - `gh release download v1.38 -R avwohl/romwbw_emu -p
'romwbw-emu_1.38_amd64.deb'` then `dpkg-deb -x`.  the packaged romwbw.html is
HEAD's template rendered apart from the three @VERSION@ substitutions, which say
1.38 where VERSION now says 1.39.  its emu_avw.rom is c7abc580, the ROM from
before e47c948, so anything that exercises the bank call answers for the old
one.  serve the UNPACKED DEB, not web/ out of
this tree: web/ held gitignored March leftovers of romwbw.js and romwbw.wasm
whose md5s differed from the CI build's, and serving them would have answered
all three checks against a five-month-old wasm.  they are deleted (make -C web
clean), as are the December 2025 romwbw_node.* and romwbw_test.* pairs that
`clean` does not name - there is no .wasm left in web/ at all, so `make serve`
and both deploy targets now fail on absent emcc instead of shipping a stale one.
what is left is a person at a browser: emcc, node and every browser are absent
from this machine.
  UPDATE 2026-09-07: that deb is still the only built page on this machine and
it no longer answers for HEAD.  romwbw.html-template was rewritten for v1.40 -
the hardcoded <option> lists are gone, and the ROM, disk and RomWBW selects are
built at load time from catalog/manifest.json beside the page - so the packaged
1.38 page and the template here are two different programs, and serving the deb
now answers the three checks above for the OLD one only.  VERSION says 1.40
where that deb says 1.38.  the deb's emu_avw.rom is still c7abc580 and still
pre-e47c948; a v1.40 package carries no ROM at all, so the next deb cannot be
served this way until `romwbw-get mirror` has been run beside its page.

- [BROWSER] the wasm's video, DSKY, sound and printer callbacks are wired to
names nothing implements.  emu_io_wasm.cc emits Module.onVideo*, Module.onDsky*
and Module.onPrinterOutput; romwbw.html-template implements Module.onVda* and
Module.onSnd*.  renaming one side does not switch it on: there is no sound
emitter in the core at all, five of the eight onVda* handlers have no
counterpart, and onVdaWriteChar takes an attribute byte the emitter never
sends.  about 200 lines on each side that have never executed - wire it
properly behind a browser, or delete one side.

- [BROWSER] nobody has opened the v1.40 page in a browser, and four things in it
are new.  the ROM, disk and RomWBW-release selects are built from
catalog/manifest.json at load time rather than from hardcoded <option> lists;
each download is checked against the manifest's size and sha256 through
crypto.subtle, which needs a secure context and says so rather than claiming a
pass it did not perform; a directory with no mirror in it gets an explanatory
status line and working file pickers instead of a 404; and the RomWBW select
disables a release this build cannot boot.  all four are covered by
tests/web_manifest.js under node, which `make -C src test` runs, and none has
been DRAWN: no browser is installed here and neither is emcc, so the wasm beside
the page has not been rebuilt since the rewrite either.  what this needs is one
person on a machine with emcc and the checklist in MANUAL_CHECKS.md.  `make -C
web serve` mirrors emu_avw and hd1k_combo into web/ before starting the server,
so a local serve has a bootable ROM and disk for the first time - that half is
worth checking too, because nothing has ever exercised it.

- [EMSCRIPTEN] the web Boot field never reads NVRAM back.  the page pushes the
string with _romwbw_set_boot_string and clears it with _romwbw_clear_nvram, and
web/makefile's EXPORTED_FUNCTIONS holds no getter, so a boot target the guest
itself writes with SYSCONF is lost on reload - the text survives only in
localStorage.  the CLI has load_nvram_setting/save_nvram_setting under
$XDG_CONFIG_HOME/romwbw_emu/nvram and the web build has no equivalent read
path.  needs emcc to build and a browser to check.

- [EMSCRIPTEN] R8's "Reading:" line names the file that was really opened on
the CLI only.  emu_host_file_get_read_name() returns "" in emu_io_wasm.cc on
purpose - a browser read is a file PICKER, so the guest's string is a hint the
user is free to ignore - and the comment there carries the whole argument.
changing it needs a name parameter on emu_host_file_provide_data() and a change
to the page's picker callback.  until then the browser answers 0xFF and R8
prints what was typed, which is what it always did.

- [EMSCRIPTEN] the page decides which RomWBW releases it can boot from a value
recorded at mirror time, not by asking the core it is running.  `romwbw-get
mirror` runs the CLI binary's --version and writes the answer into
catalog/manifest.json as emu_supported; the page disables anything outside that
list.  so the two can disagree - a mirror written by one build, served next to a
wasm compiled from another - and the page would then offer a release the wasm
refuses, or grey out one it would run.  the honest source is
emu_romwbw_supported_list(), which already exists in src/emu_init.h and
src/emu_init.cc, and emu_init.cc is already in web/makefile's ROMWBW_SRCS, so
this is one name added to EXPORTED_FUNCTIONS plus a few lines in the page.  it
was not done because it cannot be built or checked without emcc, which is absent
here, and shipping an untested export is worse than the mismatch it fixes.
tagged [EMSCRIPTEN] rather than [ANY] for that reason.
what makes it worth doing rather than merely correct: the whole point of the
mirror is that re-running it publishes a newly released disk with no wasm
rebuild.  the moment somebody re-mirrors without rebuilding - which is the
intended workflow, not an accident - emu_supported is a fact about a binary that
is no longer the one in the page.  do it the next time emcc is on the machine.

- [RELEASE] the pin those three ports carried is GONE, and what is left is
shipping.  Step 4 published the refreshed images as ioscpm v1.4.12 (2026-09-01,
29 assets, and NOT a prerelease - it is the latest release; v1.4.5 is the
prerelease): hd1k_combo.img refreshed to sha256 89b8ae1a..., the
other 19 re-uploaded byte-identical, and disks.xml's version attribute
deliberately left at 13 so the invalidation wipe never fired.  v1.4.5 is
untouched - still be19984e..., still 30 assets.
then on 2026-09-05 all three ports moved off that catalog entirely.  releaseTag
in ioscpm's EmulatorViewModel.swift, RELEASE_TAG in cpmdroid's
DiskCatalogRepository.kt and in z80cpmw's DiskCatalog.cpp are DELETED, not
repointed: each now fetches romwbw_disks' index-v0.json and reads base_url out
of the catalog document.  so "which pin does this tree carry" has no answer in
any of them any more, and any check that greps for a vN.N.N literal reports a
correct tree as broken.
what has NOT changed is the shipping half, and it is still the whole of what is
left, though not in the shape this paragraph used to claim.  the 2026-09-05
migration has shipped in none of the three - but "has not shipped the migration"
is not "still on v1.4.5", and conflating the two was the error here.  ioscpm
shipped an INTERMEDIATE repin: the App Store has served 1.5.1 since 2026-09-05,
which is build 61 (af0b9b2, submitted at e48fb80), and af0b9b2 pins releaseTag
"v1.4.12".  that pin is the FIXED R8, measured rather than inferred -
v1.4.12/hd1k_combo.img is 89b8ae1a... and carries R8's "Note: characters CP/M
cannot name became -" where v1.4.5's be19984e... does not.  so a fresh ioscpm
install already gets the fixed R8, and it is TWO of three that do not: cpmdroid
1.25 / versionCode 27 (35873d0, RELEASE_TAG "v1.4.5") and z80cpmw 1.0.23
(dbd53b1, L"v1.4.5") still fetch the erasing R8.  derive that from the commit
each store's build was cut from - not from a release tag, which none of these
three shipped builds has.  nothing here needs a release for any of it, though
no longer for the reason this used to give.  it said "this repository publishes
no disk image and fetches no catalog"; since v1.40 it fetches one -
tools/romwbw-get reads romwbw_disks' index-v0.json, the same document all three
ports now read.  the
conclusion survives the correction, and for a better reason than the old one:
the catalog is read at RUN time, so a disk published there reaches an installed
romwbw_emu with no tag cut here at all.  what a tag here is still needed for is
a new RomWBW RELEASE, because ROMWBW_SUPPORTED_RELEASES is compile-time - and
that is not what is stuck.  so no romwbw_emu tag stands between the fix and a
user.
what is behind that wall grew on 2026-09-06: e47c948 and d4f4a2a made HB_BNKCALL
work, so every ROM's bytes moved.  romwbw_disks republished all four as
generation 2 - emu_avw 3.5.1 is 4b11402a where it was c7abc580, emu_rcz80
03e64691 where it was ee3adea5 - and all three ports refreshed the ROM they
bundle to match: ioscpm 5834d92, cpmdroid 19b3563, z80cpmw cd1fb2b, which
carries two.  every one of those is 2026-09-06, later than every shipped build -
the App Store released 1.5.1 on 2026-09-05, cpmdroid 35873d0 and z80cpmw dbd53b1
are 2026-09-03 - so what a user runs is the old bank call on all three, ioscpm
included, and on cpmdroid and z80cpmw the old R8 with it.  this repository's own
shipped artifact says the same: the v1.38 deb's emu_avw.rom is c7abc580 in both
roms/ and web/, unpacked and hashed rather than assumed, which is the ROM from
before e47c948.

- [ANY] tools/check-shipped-disks.sh cannot see a built artifact on any machine
that cannot build one, and that is structural rather than a bug.  artifacts_for()
globs local paths only - dist/, bin/Release/, app/build/outputs, build/,
DerivedData - so on this machine the loop runs zero times.  MSVC, the Android SDK
and Xcode are three different machines and no one of them has all three packages.
  TWO PARTS OF THIS ARE NOW DONE, 2026-09-06.  The summary distinguished nothing
from everything: it ended on "every artifact found agrees with its own tree" after
inspecting zero packages, which reads as a pass of the half that never ran.  It now
counts what it opened and says "NO PACKAGE WAS INSPECTED, and this is NOT a pass of
the artifact half", with a different wording again for --tree-only.  Verified four
ways: no artifact, --tree-only, a planted artifact naming the v0 index (counted,
passes) and one that does not (counted, exits 1).
  And pin_of() no longer reports NO PIN FOUND at a correct tree.  All three ports
deleted their vN.N.N literal when they moved to romwbw_disks' index-v0.json.
cpmdroid's copy had grown an "index-v0" kind that asks the right question - does
this source still name the v0 index, is the legacy pin really gone, does that index
still publish the RomWBW release this build's bundled ROM declares - and its header
said the other four needed the same edit.  That copy is now canonical in all five
repositories (md5 2dd4a7f63226c2723f65b5c95e560175), with ioscpm and z80cpmw moved
into the index-v0 row.  Note z80cpmw's file is CatalogV0.cpp, not DiskCatalog.cpp
where the old pin lived.
  WHAT IS STILL OPEN is the artifact half itself, and the shape it should take has
changed since this item was written.  This item said the three authorities were the
iTunes lookup, the Play listing, and "for Windows a human-maintained note".  That
last is obsolete: z80cpmw queries DisplayCatalog now, and doing so found its own
CHANGELOG wrong by two releases.  All four ports measure their store, all four run
that check daily in CI.  So the honest remaining question is not "unzip a package"
but "does the store's version correspond to a commit whose source names the v0
index", which is a join between two checks that already exist and neither performs.
  What it must NOT do is reach for the obvious fix.  Querying
  avwohl/{z80cpmw,cpmdroid} release APIs would answer the wrong question on both:
  cpmdroid's newest GitHub release is v1.14 from 2026-01-16 while Play serves
  1.25, so the gate would scan a package nine versions older than anything a user
  has, and z80cpmw's Store channel leaves no GitHub release behind at all.
  romwbw_emu is the exception and already proves the shape works: its
  tools/check-store-version.sh --assets downloads the published .deb and reads
  emu_avw.rom out of it, which is how the shipped ROM was measured as c7abc580.
  UPDATE 2026-09-07: the artifact half has an implementation now, and this
  repository is in the table.  three things landed.  ports gained a romwbw_emu
  row keyed on tools/romwbw-get's INDEX_URL - which is why that URL is kept on
  one over-long line in the script, since a string split across two lines is
  invisible to a line grep and reads as a port that has been repointed back off
  the catalog.  the index-v0 branch gained a case for a port that bundles no ROM
  at all, because bundled_rom_of deliberately has no romwbw_emu entry: since
  v1.40 nothing is bundled, so there is no offline-first-launch hazard to check
  and "no bundled ROM" must not read as "could not read one", which is a fault.
  and scan_artifact_for learned to open a .deb with ar+tar and an .rpm with
  rpm2cpio+cpio.  that last one is not tidiness: a .deb is an ar archive holding
  data.tar.xz, so a raw byte grep of the file finds nothing that is inside it,
  and a correct romwbw_emu package was being reported as "does not name
  index-v0.json" - a red gate for a working port, which is the wolf-cry this
  script was written after.  each unpacker returns "could not look" rather than a
  verdict when its tool is missing, which is the honest answer on a stock macOS
  or Ubuntu runner with no rpm2cpio.
  WHAT IS STILL NOT COVERED is the structural gap this item opened with.
  artifacts_for() globs local build outputs, this machine builds no .deb, .msix,
  .apk or .app, and so the loop still runs zero times here and the run still ends
  on NO PACKAGE WAS INSPECTED.  the join described above - does the version the
  store serves correspond to a commit whose source names the v0 index - is still
  unbuilt.
  TWO FALSE ALARMS, BOTH SETTLED 2026-09-07, and both worth keeping written down
  because the next person will hit the same shape.  the local ../ioscpm checkout
  was at 669ce34, 24 commits behind and PREDATING ioscpm's 2026-09-05 move to
  the v0 index, so the script printed `ioscpm     NO v0 INDEX URL` and exited 1
  for a port that had migrated.  a `git -C ../ioscpm merge --ff-only origin/main`
  fixed it; check the checkout before believing that line and do not "fix" the
  table back.  the second was real and in this script: bundled_rom_of still
  named z80cpmw's roms/emu_avw.rom, and z80cpmw's own migration deleted that
  directory - it tracks no ROM at all now - so the gate printed CANNOT READ and
  exited 1 for a second working port.  its case is removed, which drops it into
  the no-bundled-ROM branch beside romwbw_emu.  the rule this leaves: a port
  that stops bundling a ROM has to lose its bundled_rom_of case in the same
  change, or this gate goes red for it.  all four ports are green as of
  2026-09-07.

- [ANY] a green pin does not mean a user has the fixed R8, and the gate implies
it does.  All three ports decide "already downloaded" by filename alone -
fileExists in ioscpm's refreshAvailableDisks, the same in cpmdroid's
DiskDownloadManager, and z80cpmw hashes nothing on download at all - and the
catalog's version attribute is 13 at v1.4.5 and at v1.4.12 alike, so nothing
re-fetches an image already on disk.  A port can be pinned correctly, shipped
correctly, and still leave every existing user on the old combo.  Whatever the
gate grows into should say which of those two questions it is answering.
what was measured while doing step 4, and is worth not re-deriving: only
hd1k_combo.img carries r8.com/w8.com at all - the other 19 published images
contain neither, so the whole job was one image and a one-line catalog diff.
the old combo's w8.com was 1280 bytes, rebuilding byte for byte from w8.asm at
3101b6d with `cp 1Ah` still in the copy loop and no `06 e9 cf` probe; its r8.com
was 1079 bytes, older than any build in this repo's history.
what would make this catchable rather than periodic: two different disks.xml
files are in play and only one of them has checksums.  this tree's
disks/disks.xml is `<disks version="6">`, 21 entries, no per-disk version or
checksum, so nothing here can look at a release asset and tell whether its
w8.com is current.  the catalog the three ports actually fetch is ioscpm's,
`<disks version="13">`, 20 entries, and it DOES carry a per-disk sha256 - and it
is that file's version attribute whose bump fires the disk-wipe hazard.

  UPDATE 2026-09-05: v0 changes the shape of this but does not close it.  Asset
  names now carry the release - hd1k_combo-v0-3.5.1.img - and every v0 catalog
  entry has a sha256, so a client CAN tell a stale image from a current one
  without a version attribute.  Whether it does is per client and still
  unsettled: ioscpm keeps a ledger and will refresh unattended, z80cpmw computes
  the verdict and calls nothing that acts on it, cpmdroid still compares
  filenames.  And the migration renames rather than re-downloads, so a migrated
  device keeps the bytes it had under the new name - which for hd1k_combo means
  a hash that disagrees with the catalog naming it.  romwbw_disks DECIDED that
  one: the pre-v0 combo is equivalent and is deliberately NOT refreshed - its
  2,342 differing bytes are CP/M slack between the same 94 identical files, and
  docs/FINDINGS.md section 10 there has the measurements.  ioscpm and z80cpmw
  implement the exemption keyed on provenance, so a downloaded image can never
  be blessed by it; cpmdroid needed no change.

  UPDATE 2026-09-05: disks/disks.xml is DELETED.  The observation above stands as
  the reason it was worthless - version="6", 21 entries, no per-disk version or
  checksum - and to it: 19 of the 21 filenames it listed name images this
  repository does not track, and one of the two it does track (hd1k_infocom.img)
  it never listed.  It was an inventory of nothing.  Nothing in the tree read it:
  verify_romwbw_pin.sh finds only *.rom/*.bin/*.img, both disk scripts hardcode
  the two-image list, and release.yml never stages disks/.  What the ports fetch
  is a different file entirely - ioscpm's published catalog - and that is what
  tools/check-shipped-disks.sh reads, over HTTP, so it is untouched by this.
  (v1.40 moved two of those three readers without changing the conclusion:
  rebuild_disk_utils.sh is deleted, and verify_disk_utils.sh takes a directory to
  scan instead of a hardcoded pair.  neither reads a .xml, and neither ever did.)

- [ANY] CROSS-REPO, DONE IN THE WORKING TREE, NOT COMMITTED: romwbw_disks'
tools/check_source_drift.sh lost both of its artifact comparisons to the v1.40
migration - each read a file this repository no longer tracks, printed an info
line and exited 0 - and both are now replaced by something stronger.
  what it does now: assemble BOTH trees' emu_hbios.asm for 3.5.1 and compare the
32 KB bank 0 (generating romwbw_ver.inc for whichever copy includes it), and
build romwbw_emu's src/{r8,w8}.asm with um80 and compare against that repo's own
build/utils/{r8,w8}.com.  no ROM, no disk image, no cpmtools, no network.  it
isolates bank 0 from the 480 KB of upstream banks that were identical by
construction, which the old whole-ROM compare could not.  measured 2026-09-07:
"emu_hbios.asm assembles to identical bank 0 in both trees (3.5.1)" and both
.com files byte-identical, PASS.
  .github/workflows/verify.yml there now sparse-checks out `src` alone in both
jobs, where it took `src roms` and `src roms disks`.
  WHAT IS LEFT: both edits are uncommitted in /Users/wohl/src/romwbw_disks.
They need reviewing and committing there.

- [ANY] docs/ROM_ATTESTATION.md is stale in three places and none of them is
safe to fix unasked.  it lives here rather than in DECISIONS.md because the work
is a document edit any machine can make; what is missing is agreement on the
wording, not a capability.  it is a signed filing to Apple - "I hereby
affirm", "I hereby grant Apple Inc. permission", "Digital Signature" - so the
signed text is not something to reword quietly, and the v1.40 migration
deliberately touched none of it.  three things in it are now out of date.  its
Component 2 says "Version: 3.5.1", which was the whole truth when a single
pinned ROM shipped and is narrower than what the
binary supports since v1.39 (3.5.1 and 3.6.0, and the version a guest sees is
read from the ROM it loads).  its "Date: December 2024" is two years stale.  and
ioscpm carries a near-identical copy of the same document that differs from this
one only in that date, so there are two signed filings and no rule about which
is the original.  the ruling is whose document it is, whether it is re-signed
today naming both releases, and whether one of the two copies goes.  what did
not need a ruling has been done: a dated note below the signature records that
the ROM has left this repository and that roms/build_emu_rom.sh reproduces the
published bytes and says so, which is what makes the GPL and attestation claim
checkable rather than asserted.  that note is additive and stops at the
signature line; everything above it is untouched.

- [ANY] DONE 2026-09-07: archive/cpm22/ is down to one tracked binary.  Six of
the seven went, each because it was rebuildable from a source beside it or a
copy of something published elsewhere; all six are in git history.
Binary/SBC_std.rom was byte-identical to the stock SBC_std.rom in the upstream
RomWBW v3.5.1 Package.zip - verified with cmp against what
`tools/romwbw-get path @upstream` fetches - so it was a RomWBW artifact after
all, which is what this migration removes.  Binary/HD0.img was zero bytes.
cpm_bios was an x86-64 ELF built from cpm_bios.cc beside it.  cpm22.sys,
cpm22asm/cpm22.sys and cpm22asm/cpm22.bin are what cpm22asm/build_cpm.sh
assembles from cpm22asm/cpm22.asm, two lines of um80/ul80, still present.
  com/mbasic.com stays and is now the whole of CI's archive/ exception: it is
third-party CP/M software with no source in this tree and no upstream this
project controls, so deleting it would lose it outright.
archive/cpm22/README.md carries the table, and test.yml's guard comment names
the one file rather than five.

- [MAC/LINUX] the last integer-narrowing warnings in a clean build are not this
repository's to fix.  qkz80_MK_INT16 in cpmemu's qkz80_types.h builds an int
and hands it to a qkz80_uint16 parameter, so qkz80_reg_pair.h is reported twice
by every object file that calls set_low()/set_high() - 8 warnings from
`make -C src all`, 12 with the test programs, none of them actionable from
here.  the fix is a qkz80_uint16() around that macro body, in cpmemu; CI clones
cpmemu's default branch, so this goes quiet on the first build after that lands,
with nothing to bump here.  until then a clean build is not silent, which is the
state that teaches people to stop reading warnings.  measured again 2026-09-01
against cpmemu v4.7.2 (91151f1), which is what v1.37 shipped against: same 8 and
12, all of them qkz80_reg_pair.h:32 and :35.  nothing about v1.37 touched this -
`git diff 9a94e8d 91151f1 -- 'src/qkz80*.h' 'src/qkz80*.cc'` is empty, so the
macro is byte for byte what it was.  measured again 2026-09-06 with clang++
against cpmemu ed778f7, which is v4.8.0 - a cpmemu release has landed since and
did not touch it: same 8 from `make -C src all`, both lines again, and
`git diff 91151f1 ed778f7 -- 'src/qkz80*.h' 'src/qkz80*.cc'` is empty.
z80cpmw's C4267 suppression on hbios_dispatch.cc is removable either way: 8eeb227 cast all six sites here, so
the file is clean at
/W3 from v1.37 on.  it has NOT been removed there - checked 2026-09-06 at
z80cpmw cd1fb2b, still present at z80cpmw/z80cpmw.vcxproj:297 (it was :260 at
148be4f; the line moved, the suppression did not).  grep for the
MSBuild spelling `<DisableSpecificWarnings>4267`, not for `/wd4267`; searching
for the compiler flag reports it gone when it is not, which is how this entry
nearly got deleted as stale.

- [WINDOWS] nothing on Windows is RUN, only compiled.  the msvc job builds the
three core .cc files z80cpmw pulls straight out of src/ and stops there: there
is no MSVC build of the emulator to link and no test program that would run.
the only thing that executes Windows code is z80cpmw built and run by hand on a
Windows box (build.bat, run_test.bat, tests/): that port has no CI at all -
`gh api repos/avwohl/z80cpmw/actions/workflows` answers 0 and `gh run list` is
empty.  cpmemu is no longer in that position: 43bff79 added
.github/workflows/ci.yml on 2026-09-03, whose ubuntu and macos jobs run
tests/run_tests.sh --require and whose windows-latest job builds with MSVC and
then runs tests\win_console.bat under CPMEMU_REQUIRE_MSVC=1 - the same batch
file that suite's "windows console" section shells out to, so those cases are
finally executed by a machine, and the file's header says that job is the first
one ever to run them.  the other of that suite's two Windows sections, "windows
cross-compile", skips merely for want of x86_64-w64-mingw32-g++, which IS
installed here, so it runs and is another compile rather than a run.  what is
still only compiled and never run is this repository's own core.
