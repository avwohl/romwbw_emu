Todo list.  Open items only - finished work is in CHANGELOG.md and in the commit
messages that made the changes, not kept here.  Checks that need a person at a
keyboard are in MANUAL_CHECKS.md.  Questions that need the owner's ruling - not
a check to run but a call to make - are in DECISIONS.md, because a [DECISION]
tag was never a machine capability and so broke the promise the next paragraph
makes about every tag in this file.

Every item carries a tag saying what a machine has to be able to do to take it,
so a session on another OS can see at a glance what it can pick up.

EVERY ITEM IS WORK IN THIS REPOSITORY.  An item whose work would be done in
romwbw_disks, z80cpmw, ioscpm or cpmdroid belongs in THAT repository's
todo.txt, where the session that can close it will read it - filing it here
makes a list nobody can act on and that nobody closes, because the person
finishing the work is not looking at this file.  That is what 342ac22
("refile two items into the repos that would close them") established, and
two entries were filed here against it on 2026-09-17 and removed the same
day: one about the three GUI clients dropping the release gate, and one about
romwbw_disks' documentation of it.  Both were real work; neither was this
repository's.

A cross-repository CONSEQUENCE is different from a cross-repository TASK, and
belongs in DOWNSTREAM.md - which is exactly where "what a port must change"
already lives.

WHAT THIS MACHINE ACTUALLY HAS, measured 2026-09-17 and not inferred:

  node        YES, v26.8.2 (brew, installed 2026-09-17 to run the web JS tests)
  emcc        NO.  Not in `brew list`, not on PATH.  So `make -C web` and
              `make -C web check` do NOT run here, and web/ holds no .wasm.
  python3     Apple 3.9.6 at /usr/bin/python3.  There is NO ~/bin shim, so
              plain `python3` is 3.9.6; /opt/homebrew/bin/python3.14 for 3.14.
  um80/ul80   YES, ~/Library/Python/3.14/bin
  Chrome      YES, and headless Chrome is drivable with nothing installed -
              see the [BROWSER] note below.
  newest tag  v1.42.  There is NO v1.43 tag: WIP.md records that v1.43 was
              committed and the GitHub release never cut, blocked on a dead PAT.

  A previous revision of this header said emcc had been installed here on
  2026-09-17 and that node had been upgraded as its dependency.  Both were
  false when written; the emcc half still is.  [EMSCRIPTEN] means "not on this
  machine".  [BROWSER] means "needs a person, or the headless-Chrome trick".

- [BROWSER] no browser has drawn the page since a zero-byte export started
downloading and since the download name became the lowercased last component of
the typed path.  the checklist is in MANUAL_CHECKS.md.
  WHAT IS ALREADY SETTLED, so nobody re-does it: the xterm viewport WAS drawn
headless on 2026-09-15 (MANUAL_CHECKS.md records it), and the RomWBW release
select was drawn headless on 2026-09-17 - and that select has since been
DELETED along with the release gate, so that check is gone rather than passed.
  WHAT TO SERVE.  `make -C web serve` is the best answer and does not work on
this machine: no emcc, so no wasm.  Failing that, the newest deb is v1.42
(2026-09-13); its web/romwbw.html is byte-identical to
`sed s/@VERSION@/1.42/ web/romwbw.html-template` at tag v1.42, it carries a
freshly built wasm and vendor/, and it carries NO ROM - so `romwbw-get mirror`
has to be run beside it.  It is now several commits behind HEAD, including the
whole v1.44 release-gate change, so it answers for the two download checks and
for nothing about the release select.

- [BROWSER] the wasm's video, DSKY, sound and printer callbacks are wired to
names nothing implements.  MEASURED 2026-09-17, and the numbers this item used
to carry were all wrong:
  emu_io_wasm.cc emits 14 Module.on* names; romwbw.html-template assigns 19;
exactly 6 appear on both sides (onConsoleOutput, onStatus, onLog, onError,
onHostFileRequestRead, onHostFileDownload).
  8 emitted names reach no handler: onPrinterOutput, onDskyHex, onDskySegments,
onDskyLeds, onDskyBeep, onVideoClear, onVideoSetCursor, onVideoWriteChar.
  12 assigned handlers are never called: eight onVda*, four onSnd*.
  renaming does not switch it on.  onVideoClear->onVdaClear and
onVideoSetCursor->onVdaSetCursor are arity-identical and WOULD work;
onVideoWriteChar->onVdaWriteChar draws invisibly, because the page reads an
attribute byte the emitter never sends and `undefined & 0x0F` is 0; and five
onVda* and all four onSnd* have no emitter at all.
  "there is no sound emitter in the core" was WRONG: hbios_dispatch.cc's
HBF_SNDPLAY and HBF_SNDBEEP both call emu_dsky_beep(), which emits
Module.onDskyBeep(ms).  It carries a duration and no frequency, which is the
[SOUND] item below, not this one.
  the real sizes are 114 lines on the core side and 150 on the page, not "about
200 each" - AND there is a SECOND dead copy: web/romwbw-debug.html carries the
same onVda*/onSnd* block, 152 more lines.  deleting one side means deleting it
twice.  wire it properly behind a browser, or delete it.

- [BROWSER] three of the four things that were new in the page are still
undrawn: the manifest-built ROM/disk selects, the crypto.subtle size+sha256
check (which needs a secure context and says so rather than claiming a pass it
did not perform), and the no-mirror directory case with its explanatory status
line and working file pickers.  all three are covered by tests/web_manifest.js
under node, which RUNS here now, and none has been DRAWN.
  the fourth - the RomWBW select that greyed out a release the core could not
boot - was drawn headless on 2026-09-17 and then deleted in v1.44.  the core
has no opinion about releases any more, so there is nothing to grey out.
  THE BROWSER IS DRIVABLE FROM HERE with nothing installed: Chrome's DevTools
endpoint is plain HTTP plus a WebSocket, node has a global WebSocket, so
`--headless=new --remote-debugging-port=N` plus Runtime.evaluate drives the real
page - no puppeteer, no npm.  Both checks of 2026-09-17 were run that way.  A
harness was not committed; whether one should be is the real decision hiding
behind "needs a person at a browser", and it is open.  What still genuinely
needs a human is the checks that turn on a browser DEFAULT ACTION - a download,
a paste, a zoom - which is most of MANUAL_CHECKS.md.
  ALL OF IT NEEDS emcc, which this machine does not have.

- [EMSCRIPTEN] R8's "Reading:" line names the file that was really opened on
the CLI only.  emu_host_file_get_read_name() returns "" in emu_io_wasm.cc on
purpose - a browser read is a file PICKER, so the guest's string is a hint the
user is free to ignore - and the comment there carries the whole argument.
changing it needs a name parameter on emu_host_file_provide_data() and a change
to the page's picker callback.  until then the browser answers 0xFF and R8
prints what was typed, which is what it always did.

- [ANY] src/emu_hbios.asm is bank 0 of every published ROM, and it is now
BYTE-IDENTICAL to romwbw_disks' copy - tools/check_source_drift.sh there
asserts exactly that, where it used to assert the two differed in a documented
way.  so an edit in either tree is an edit to the other, and to every published
ROM's sha256, which bumps each romwbw_disks version's generation, and a
generation bump is what makes a client invalidate its cached images - every
user re-downloads their disks.  that is not theoretical: HB_BNKCALL (e47c948,
d4f4a2a, 2026-09-06) cost exactly that, a generation-2 republish of both 3.5.1
and 3.6.0.  budget it before the next emu_hbios.asm change.
  the file no longer names a release: both version stamps come from a generated
romwbw_ver.inc.  making it so moved no published byte, because published ROMs
are built from romwbw_disks' copy, which was already parameterised.
  DO NOT BUILD ROMs HERE as a matter of routine.  They are romwbw_disks'
province; nothing in this repository's build, test or release path assembles
one.  roms/build_emu_rom.sh is a DEBUGGING tool for when bank 0 is actually
suspect - what continuously asserts the two trees agree is
romwbw_disks/tools/check_source_drift.sh, which needs no ROM.
  the finished HB_BNKCALL work is CHANGELOG.md's `### HB_BNKCALL performs a
real bank call` section (grep it; CHANGELOG grows at the top, so a line number
will not stay right).

- [ANY] docs/ROM_ATTESTATION.md is a signed filing to Apple - "I hereby
affirm", "I hereby grant Apple Inc. permission", "Digital Signature" - so the
signed text is not something to reword quietly, and what is open is agreement
on the wording rather than a capability.  TWO of the three stale things are
now fixed, because they were statements of technical fact that had become
FALSE rather than parts of the affirmation: its claim that the emulator
"refuses a ROM whose release is not on" a list, and its claim that
build_emu_rom.sh "reproduces RomWBW 3.5.1 only".  Both were corrected in v1.44
and the affirmation and signature block were not touched.
  WHAT IS STILL OPEN, and needs the owner: its "Date: December 2024" is two
years stale, and ioscpm carries a near-identical copy of the same document that
differs from this one only in that date - so there are two signed filings and
no rule about which is the original.  The ruling is whose document it is,
whether it is re-signed today, and whether one of the two copies goes.

- [WINDOWS] nothing on Windows is RUN, only compiled.  the msvc job builds the
three core .cc files z80cpmw pulls straight out of src/ and stops there: there
is no MSVC build of the emulator to link and no test program that would run.
the only thing that executes Windows code is z80cpmw built and run by hand on a
Windows box (build.bat, run_test.bat, tests/): that port has no CI at all.
cpmemu is no longer in that position: 43bff79 added .github/workflows/ci.yml on
2026-09-03, whose ubuntu and macos jobs run tests/run_tests.sh --require and
whose windows-latest job builds with MSVC and then runs tests\win_console.bat
under CPMEMU_REQUIRE_MSVC=1 - so those cases are finally executed by a machine.
what is still only compiled and never run is this repository's own core.

- [SOUND] the SND handler stores four channels and plays one beep.  handleSND
in src/hbios_dispatch.cc keeps snd_period[4] and snd_volume[4], and
BF_SNDQUERY/SNDQ_CHCNT answers "4 tone channels, 0 noise" - but nothing reads
those arrays back out.  BF_SNDPLAY looks at channel 0 only and calls
emu_dsky_beep(duration), which is a fixed beep of that length: the period is
not a pitch and the other three channels are inaudible.  a guest playing a
four-voice tune hears four identical beeps in sequence.  SNDQ_DEV already
declines to name a chip for exactly this reason.
  WHAT WOULD CLOSE IT is rendering the channels the handler already records:
period -> frequency, volume -> amplitude, mixed across the four.  that is one
AY-3-8910/YM2149 worth of square-wave generation and it belongs here, in the
shared core, not in any one front end - all four ports would get it from the
same change, and only the output side (AudioTrack, AVAudioEngine, WASAPI, Web
Audio) is per-client.
  IT NEEDS A NEW emu_io ENTRY POINT.  src/emu_io.h declares exactly one audio
hook, `void emu_dsky_beep(int duration_ms)`, and a duration is the whole of its
vocabulary - there is no way to hand a front end samples or a pitch through it.
so this is a DOWNSTREAM.md-sized change, not a local one: adding the hook is
the easy half and four ports implementing it is the rest.  a front end that
does not want it must keep working, so the new hook needs a default the way the
optional ones already here do.
  requested by a user on a physical tablet against cpmdroid, where it sat in
docs/bug1.txt until 2026-09-15.  cpmdroid cannot close it alone.
  THE SAME REPORT ASKED ABOUT MIDI and cpmdroid/docs/midi.md is the research
that came of it: RomWBW has no native MIDI in HBIOS, and MIDI on RC2014/Z80
hardware is an external module.  Read it before treating MIDI as a second
feature - it is a different question from rendering the SND channels, and only
this one has a driver interface already sitting in handleSND.
